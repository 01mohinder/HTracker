# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# MONGODB_URI: Connection string for MongoDB (e.g. mongodb+srv://username:password@cluster.mongodb.net/ht_grind)
MONGODB_URI=

# ADMIN CONFIGURATION:
# Comma-separated list of admin email addresses granted elevated permissions.
ADMIN_EMAILS=
# Optional static administrative key for automation/CLI webhooks (sent via X-Admin-Key header)
ADMIN_SECRET_KEY=

# Custom Firebase Configuration (Optional: for deploying to Vercel/Netlify with your own Firebase Project)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
